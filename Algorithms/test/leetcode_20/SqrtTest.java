package leetcode_20;

import static org.junit.Assert.*;

import org.junit.Test;

public class SqrtTest {

	@Test
	public void test() {
		Sqrt s = new Sqrt();
		assertEquals(s.sqrt(3), 1);
		assertEquals(s.sqrt(4), 2);
	}

}
